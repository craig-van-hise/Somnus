
# Phased Development Plan — [Status Marker] ✅

## 1. Project Context & Strict Constraints

This is a micro-refinement cycle for Somnus v1.5. The objective is to adjust the psychoacoustic boundaries of the Acoustic Entrainment Subsystem. Specifically, we must extend the Master Bus start fade-in to 10 seconds to ensure a gentler auditory onset, and we must lower the Master LPF floor to 150 Hz to create a darker, more submerged acoustic environment during the deep sleep phase.

## 2. Enforced Dependencies & Architecture

* **Master Fade-In:** `GenerativeAudioController.js` must execute `this.masterBus.volume.rampTo(0, 10)` during `startSession()`.
* **LPF Floor:** `lifecycleRampEngine.js` must return `150` (instead of `300`) when `timeMinutes >= solTargetMinutes` in the `calculateLpfCutoff` function, and adjust its exponential decay floor accordingly.
* **TDD Enforcement:** The coding agent is **STRICTLY PROHIBITED** from altering the provided test code. The agent must copy the literal Javascript blocks into the test files and write the implementation to make them pass.

## 3. Implementation Phases

### Phase 1: Deep Sleep LPF Floor Adjustment (150Hz) — [Status Marker] ✅

* **Objective:** Darken the hold phase of the session by lowering the LPF floor from 300Hz to 150Hz.
* **Implementation Directives:**
* In `lifecycleRampEngine.js`, update the `calculateLpfCutoff` function.
* Change the floor value returned when the session target is reached from `300` to `150`.
* Ensure the exponential decay math correctly targets this new `150` Hz floor.


* **TDD Implementation:**
* *Instruction to Agent:* "Create `src/test/phase1_lpf_floor.test.js`. Copy and paste the following literal test code exactly. Write the implementation code in `lifecycleRampEngine.js` until this test passes."


```javascript
import { describe, it, expect } from 'vitest';
import { calculateLpfCutoff } from '../engine/lifecycleRampEngine';

describe('Phase 1: Deep Sleep LPF Floor Adjustment', () => {
  it('should strictly floor at 150Hz after the SOL target is reached', () => {
    // timeMinutes (25) is greater than solTargetMinutes (20)
    const cutoff = calculateLpfCutoff(0.5, 25, 20); 
    expect(cutoff).toBe(150);
  });
});

```


* **Execution & HITL Protocol:**
* "Transition Phase status to 🔵 Active."
* "Write implementation and execute TDD tests. Once tests pass, update status to ✅ Completed."



### Phase 2: Gentle Master Fade-In (10 Seconds) — [Status Marker] ✅

* **Objective:** Ensure the audio engine rolls in incredibly smoothly over 10 seconds to prevent startling the user's nervous system.
* **Implementation Directives:**
* In `GenerativeAudioController.js` -> `startSession()`, locate the master bus fade-in line.
* Change `this.masterBus.volume.rampTo(0, 3)` to `this.masterBus.volume.rampTo(0, 10)`.


* **TDD Implementation:**
* *Instruction to Agent:* "Create `src/test/phase2_gentle_fade.test.jsx`. Copy and paste the following literal test code exactly. Write the implementation code in `GenerativeAudioController.js` until this test passes."


```javascript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { audioController } from '../engine/GenerativeAudioController';

describe('Phase 2: Gentle Master Fade-In', () => {
  beforeEach(async () => {
    audioController.isInitialized = false;
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: false,
      headers: new Headers({ 'content-type': 'text/html' })
    }));
    await audioController.bootEngine();
  });

  afterEach(() => {
    vi.unstubAllGlobals();
    vi.restoreAllMocks();
  });

  it('should fade in smoothly over 10 seconds on start instead of 3 seconds', async () => {
    const rampSpy = vi.spyOn(audioController.masterBus.volume, 'rampTo');

    await audioController.startSession();

    // Verify the duration parameter (second argument) is strictly 10
    expect(rampSpy).toHaveBeenCalledWith(0, 10);
  });
});

```


* **Execution & HITL Protocol:**
* "Transition Phase status to 🔵 Active."
* "Write implementation and execute TDD tests. Once tests pass, update status to ✅ Completed."
* 🚨 **[HITL PAUSE REQUIRED]** The resulting Application must be validated. Pause execution and instruct the user to refresh `localhost:5173`. Hit Play. Verify that the volume swells in much more gradually over a full 10 seconds. Move the "Current State" slider all the way to "Deep Sleep" (right) and verify the Nature sounds are noticeably darker and more submerged than they were previously. Wait for the user's explicit `VALIDATED` confirmation.



## 4. Final Review & Orchestration

* **Status Reconciliation:** Verify all phase markers match ✅ or ❌ before updating the top-level status to ✅.
* **Final Delivery:** Present the completed, fully tested product to the user.