
The Tone.js context is successfully extracting, but the browser is now throwing an `InvalidAccessError` at the connection block inside `setupReverbWorklet`.
> **The Problem:** You are trying to connect the native `this.reverbWorklet` to `this.masterBus`. `this.masterBus` is a Tone.js wrapper object. Native Web Audio nodes cannot connect to Tone.js objects; they can only connect to other native nodes.
> **The Solution:** Remove your existing connection block at the bottom of `setupReverbWorklet` (the `Tone.connect` and `if (this.masterBus)` logic). Replace it entirely with this exact bridging code to route the native node directly to the hardware speakers:
> ```javascript
> // 1. Intercept the Tone.js signal
> if (this.reverbInputBus) {
>     try {
>         this.reverbInputBus.disconnect(); // Disconnect from the old Tone wrapper
>     } catch (e) {}
>     
>     // Route Tone.js -> Native Worklet (Tone supports this direction natively)
>     this.reverbInputBus.connect(this.reverbWorklet);
> }
> 
> // 2. Route Native Worklet -> Hardware Speakers
> // Bypass Tone.js completely on the output to avoid InvalidAccessError.
> // We will create a native GainNode to act as our -6dB master headroom.
> const nativeGain = nativeContext.createGain();
> nativeGain.gain.value = 0.5; 
> 
> this.reverbWorklet.connect(nativeGain);
> nativeGain.connect(nativeContext.destination);
> 