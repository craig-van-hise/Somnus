

# Phased Development Plan — ✅ Completed

## 1. Project Context & Strict Constraints

This is a micro-fix to align the audio engine's network fetch paths with the user's local file system extensions.

## 2. Enforced Dependencies & Architecture

* **Asset Routing:** The Ocean Waves file is an `.mp3`, not a `.wav`. The `safeLoadBuffer` URI must be updated to reflect this.
* **TDD Enforcement:** The coding agent is **STRICTLY PROHIBITED** from altering the provided test code.

## 3. Implementation Phases

### Phase 1: Extension Alignment — ✅ Completed

* **Objective:** Update the ocean waves fetch URL to request the `.mp3` file.
* **Implementation Directives:**
* In `GenerativeAudioController.js` -> `bootEngine()`, locate the `safeLoadBuffer` call for the waves player.
* Change the requested URL from `ocean.wav` to `ocean.mp3`.
* *Example:* `const wavesBuffer = await this.safeLoadBuffer(encodeURI('/Nature Sounds Audio/Ocean Waves/ocean.mp3'));`


* **TDD Implementation:**
* *Instruction to Agent:* "Create `src/test/phase1_extension.test.jsx`. Copy and paste the following literal test code exactly. Write the implementation code in `GenerativeAudioController.js` until this test passes."


```javascript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { audioController } from '../engine/GenerativeAudioController';

describe('Phase 1: Extension Alignment', () => {
  beforeEach(() => {
    audioController.isInitialized = false;
  });

  it('should request ocean.mp3 instead of ocean.wav', async () => {
    const fetchSpy = vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: false, // Force it to fail gracefully so we don't need real buffers
      headers: new Headers({ 'content-type': 'text/html' })
    }));

    await audioController.bootEngine();

    const oceanCall = fetchSpy.mock.calls.find(call => call[0].includes('ocean.mp3'));
    expect(oceanCall).toBeDefined();

    vi.unstubAllGlobals();
  });
});

```


* **Execution & HITL Protocol:**
* "Transition Phase status to 🔵 Active."
* "Write implementation and execute TDD tests. Once tests pass, update status to ✅ Completed."


