
The `InvalidAccessError` means you are creating the `AudioWorkletNode` on a different `AudioContext` than the one `this.reverbInputBus` is using. Because Tone.js manages its own contexts under the hood, global extraction is causing a cross-context mismatch. You must extract the context directly from the source node to guarantee they match perfectly.
> Replace your entire context extraction, Worklet creation, and connection logic inside `setupReverbWorklet` with this exact code block:
> ```javascript
> // 1. Extract context DIRECTLY from the node we are connecting from.
> // This absolutely guarantees both nodes exist in the same AudioContext.
> const toneCtx = this.reverbInputBus.context;
> const nativeContext = toneCtx.rawContext || toneCtx._context || toneCtx;
> 
> if (!nativeContext) {
>     throw new Error("CRITICAL: Failed to extract matched native context from input bus.");
> }
> 
> // 2. Add module and construct Worklet on the matched context
> await nativeContext.audioWorklet.addModule('/assets/fdn-reverb-worklet.js');
> this.reverbWorklet = new AudioWorkletNode(nativeContext, 'fdn-reverb-worklet', {
>     numberOfInputs: 1,
>     numberOfOutputs: 1,
>     outputChannelCount: [2],
> });
> this.reverbNode = this.reverbWorklet;
> 
> // 3. Disconnect Tone node from its old path
> try {
>     this.reverbInputBus.disconnect();
> } catch (e) {}
> 
> // 4. Bridge Tone.js to Native Worklet safely
> // Tone.connect is the only safe way to bridge Tone objects to Native objects
> Tone.connect(this.reverbInputBus, this.reverbWorklet);
> 
> // 5. Bridge Native Worklet to Hardware Speakers
> // Keep this strictly native to avoid InvalidAccessError going backwards
> const nativeGain = nativeContext.createGain();
> nativeGain.gain.value = 0.5; // -6dB headroom
> 
> this.reverbWorklet.connect(nativeGain);
> nativeGain.connect(nativeContext.destination);
