

The `InvalidAccessError` is happening because `GenerativeAudioController` is a singleton caching old Worklets during React hot-reloads. You are trying to connect new Tone.js nodes to a Worklet from a dead AudioContext.
> You MUST completely overwrite the `setupReverbWorklet` function with this exact code block. It forces a cache wipe, builds fresh native nodes on the strictly matched context, and bypasses Tone.js on the output.
> ```javascript
> async setupReverbWorklet() {
>     // 1. NUKE THE CACHE. Prevent hot-reload cross-context contamination.
>     this.reverbWorklet = null;
>     this.reverbNode = null;
> 
>     // 2. Extract context DIRECTLY from the input bus to guarantee universe alignment.
>     const toneCtx = this.reverbInputBus.context;
>     const nativeContext = toneCtx.rawContext || toneCtx._context || toneCtx;
> 
>     if (!nativeContext) {
>         throw new Error("CRITICAL: Failed to extract native context.");
>     }
> 
>     try {
>         // 3. Build a fresh Worklet strictly on the active context
>         await nativeContext.audioWorklet.addModule('/assets/fdn-reverb-worklet.js');
>         this.reverbWorklet = new AudioWorkletNode(nativeContext, 'fdn-reverb-worklet', {
>             numberOfInputs: 1,
>             numberOfOutputs: 1,
>             outputChannelCount: [2],
>         });
>         this.reverbNode = this.reverbWorklet;
> 
>         // 4. Sever old Tone.js routing
>         try {
>             this.reverbInputBus.disconnect();
>         } catch (e) {}
> 
>         // 5. Bridge Tone.js to Native Worklet
>         Tone.connect(this.reverbInputBus, this.reverbWorklet);
> 
>         // 6. Bridge Native Worklet to Hardware Speakers natively (Bypass Tone.js wrappers entirely)
>         const nativeGain = nativeContext.createGain();
>         nativeGain.gain.value = 0.5; // -6dB headroom
>         
>         this.reverbWorklet.connect(nativeGain);
>         nativeGain.connect(nativeContext.destination);
>         
>     } catch (err) {
>         console.error('CRITICAL REVERB SETUP ERROR:', err);
>     }
> }
>