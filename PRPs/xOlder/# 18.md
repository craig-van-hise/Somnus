

# Phased Development Plan — [Status Marker] ✅

## 1. Project Context & Strict Constraints

This is a corrective development cycle for Somnus v1.5. The objective is to resolve critical audio artifacts and state-binding failures introduced during the initial Acoustic Entrainment Subsystem pivot. Specifically, the system must enforce strict monophonic spatial positioning on the Isochronic Tremolo node and establish a direct, real-time data binding between the React `AppContext` fader states and the `GenerativeAudioController` Tone.js volume nodes.

## 2. Enforced Dependencies & Architecture

* **Audio Engine:** Tone.js (v15.0.4). `Tone.Tremolo` MUST be explicitly configured with `spread: 0`.
* **State Management:** React (v18.3.1) Context API. `AppContext` must strictly synchronize `mixerState` with `enginePayload` and directly invoke real-time updates to the audio graph to prevent fader latency.

## 3. Implementation Phases

### Phase 1: Spatial Centering Correction (Audio Topology) — [Status Marker] ✅ Completed

* **Objective:** Eliminate the unintended stereo auto-panning artifact on the Isochronic channel (Channel 2).
* **Implementation Directives:**
* In `GenerativeAudioController.js` -> `bootEngine()`, locate the initialization of `this.ch2Tremolo`.
* Refactor the initialization to use a configuration object: `new Tone.Tremolo({ frequency: 4, depth: 0.8, spread: 0 })` to enforce a strict mono-centered amplitude modulation.
* Ensure `this.ch2Tremolo` is properly routed to `this.ch2Volume`.


* **TDD Implementation:**
* *Instruction to Agent:* "Write the implementation code to satisfy the following test. Do not proceed until this test executes and passes."
* **Test 1: Tremolo Spread is Zero**
* *Setup/Mocking:* Boot the `GenerativeAudioController` in a testing environment and fetch the topography via `getAudioGraphTopography()`.
* *Action:* Access `ch2Tremolo`.
* *Assertion:* `ch2Tremolo.spread` must strictly equal `0`.




* **Execution & HITL Protocol:**
* "Transition Phase status to 🔵 Active."
* "Write implementation and execute TDD tests. Once tests pass, update status to ✅ Completed."



### Phase 2: React Context to Engine Payload Synchronization — [Status Marker] ✅ Completed

* **Objective:** Repair the broken data binding in `AppContext.jsx` so that manual fader adjustments update the `enginePayload` and trigger the audio controller.
* **Implementation Directives:**
* In `AppContext.jsx`, modify `updateMixerState`. When the local `mixerState` is updated, it must concurrently update `enginePayload.mixerState`.
* Ensure that updating `mixerState` directly calls the `onParamUpdate` callback if it exists, passing the newly merged payload, so the audio engine immediately receives the new volumes without waiting for the next 1-second automation tick.


* **TDD Implementation:**
* *Instruction to Agent:* "Write the implementation code to satisfy the following tests. Do not proceed until these tests execute and pass."
* **Test 1: updateMixerState Updates Engine Payload**
* *Setup/Mocking:* Render `<AppProvider>` with a mock `onParamUpdate` function.
* *Action:* Call `updateMixerState({ ch1Volume: -20 })` from a consumer component.
* *Assertion:* `enginePayload.mixerState.ch1Volume` must immediately or eventually (if debounced) reflect `-20`.


* **Test 2: Callback Invocation on Fader Change**
* *Setup/Mocking:* Render `<AppProvider>` with a mock `onParamUpdate(spy)`.
* *Action:* Call `updateMixerState({ ch3Volume: -15 })`.
* *Assertion:* The `spy` function must be called, and the passed payload must contain `mixerState: { ch3Volume: -15, ... }`.




* **Execution & HITL Protocol:**
* "Transition Phase status to 🔵 Active."
* "Write implementation and execute TDD tests. Once tests pass, update status to ✅ Completed."



### Phase 3: Real-Time Audio Node Binding & Generative Muting — [Status Marker] ✅ Completed

* **Objective:** Ensure the `GenerativeAudioController` exposes a real-time update method for the UI and properly silences the paused Generative channels.
* **Implementation Directives:**
* In `GenerativeAudioController.js`, extract the fader update logic from `processAutomationTick` into a new method: `applyMixerState(mixerState)`. Call this new method from inside `processAutomationTick` to maintain the automation loop.
* In `bootEngine()`, initialize `this.ch5Volume` to `new Tone.Volume(-Infinity)` by default to ensure the paused Generative Music drone layers are strictly muted unless explicitly raised by the user in the debug UI.


* **TDD Implementation:**
* *Instruction to Agent:* "Write the implementation code to satisfy the following tests. Do not proceed until these tests execute and pass."
* **Test 1: applyMixerState Mutates Volume Nodes**
* *Setup/Mocking:* Boot the audio controller.
* *Action:* Call `applyMixerState({ ch1Volume: -12, ch2Volume: -24 })`.
* *Assertion:* `ch1Volume.volume.value` equals `-12`, and `ch2Volume.volume.value` equals `-24`.


* **Test 2: Generative Muting Default**
* *Setup/Mocking:* Boot the audio controller.
* *Action:* Check initial values.
* *Assertion:* `ch5Volume.volume.value` must equal `-Infinity` to prevent generative bleed.




* **Execution & HITL Protocol:**
* "Transition Phase status to 🔵 Active."
* "Write implementation and execute TDD tests. Once tests pass, update status to ✅ Completed."
* 🚨 **[HITL PAUSE REQUIRED]** The resulting Audio must be validated. Pause execution and instruct the user to open the application (`localhost:5173`) and hit Play. Instruct the user to open the Advanced Settings cog. Move all 5 faders independently. The volume must respond immediately in real-time, and the wild stereo panning effect must be entirely gone. Wait for the user's explicit `VALIDATED` confirmation before proceeding.



## 4. Final Review & Orchestration

* **Status Reconciliation:** Verify all phase markers match ✅ or ❌ before updating the top-level status to ✅.
* **Final Delivery:** Present the completed, fully tested product to the user.